name = "dvd-library"
type = "javascript"
compatibility_date = "2026-08-04"

# Configuración para sitio estático (Cloudflare Pages)
[site]
bucket = "./dist"

# Build settings
[build]
command = "npm run build"
cwd = "./"
